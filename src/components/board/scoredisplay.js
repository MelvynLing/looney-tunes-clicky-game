import React from 'react';

const ScoreDisplay = (props) => (<h2 className="score-keeper">Score: {props.score}</h2>);

//const HighScore = (props) => (<div className="high-score">High Score:{props.highscore}</div>);

export default ScoreDisplay;